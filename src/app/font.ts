import {Roboto} from "next/font/google";

export const lightRoboto = Roboto({
    weight: '300',
    subsets: ['latin'],
    display: 'swap',
})

export const mediumRoboto = Roboto({
    weight: '400',
    subsets: ['latin'],
    display: 'swap',
})